# RTT_T2 runtime dependencies
# Recommended Python: 3.9+
# Platform: Windows only
#
# External requirement (not installable via pip):
#   SEGGER J-Link software — provides JlinkArm.dll for RTT/J-Link features
#
# pyqtgraph patch (optional, see docs/source_code.md):
#   sigMousePress / sigMouseRelease on GraphicsScene for waveform drag UX

PySide6>=6.5,<7
pylink-square==1.1.0
pyserial==3.5
pyqtgraph>=0.13.7,<0.15
numpy>=1.19,<2
requests>=2.28,<3
pyte>=0.8.2,<0.9
