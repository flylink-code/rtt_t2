# RTT_T2 runtime dependencies
# Recommended Python: 3.9 (see docs/source_code.md)
# Platform: Windows only
#
# External requirement (not installable via pip):
#   SEGGER J-Link software — provides JlinkArm.dll for RTT/J-Link features
#
# Manual patches required after install (see docs/source_code.md):
#   - PySimpleGUI: add get_vscroll_position() to Element class
#   - pyqtgraph: add sigMousePress / sigMouseRelease to GraphicsScene
#
# Note: PySimpleGUI 4.53.0 (original dev version) is no longer on PyPI;
#       4.60.5.1 is the closest available 4.x release.

PySimpleGUI==4.60.5.1
pylink-square==1.1.0
pyserial==3.5
pyqtgraph==0.12.4
PyQt5>=5.12,<6
numpy>=1.19,<2
requests>=2.28,<3
keyboard>=0.13.5
